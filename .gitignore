/evuproxy
*.tgz
.DS_Store

# Local Docker Compose overrides (copy from .env.example)
.env
contrib/crowdsec/.env
contrib/crowdsec/.install-mode
contrib/crowdsec/vendor/

# Private / local-only docs (not published in this repo)
.cursor/
local_docs/

# Dev mock API persisted config (docker-compose.dev.yml bind-mount)
docker/mock-api/state/

# Python (e.g. docker/mock-api when run locally)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
env/
*.egg-info/
.eggs/
dist/
build/

# CodeQL
.codeql-evuproxy-db/
.codeql-security-extended.csv
.codeql-code-scanning.sarif
